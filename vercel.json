{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.x",
  "installCommand": "bun install && cd packages/schema && bun install && cd ../../apps/server && bun install && cd ../web && bun install && cd ../..",
  "buildCommand": "cd packages/schema && bun run build && cd ../../apps/web && bun run build && cd ../.. && bun build apps/server/src/index.ts --outdir api --target bun --external sharp",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/health", "destination": "/api" },
    { "source": "/docs", "destination": "/api" },
    { "source": "/openapi.json", "destination": "/api" },
    { "source": "/uploads/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
